# 可选：开新分支做这一版功能（避免直接堆在 main 上）
git checkout -b feature/doc-images-and-routing

# 只加入要进版本库的源码与配置（不要 git add . 把本地库、密钥全加进去）
git add .gitignore app/ tests/ frontend/ doc/ AGENTS.md Makefile .env.example

# 按需补充其它你确定要提交的路径

git status   # 再检查一遍暂存列表

git commit -m "feat: 文档插图与检索路由（MCP/意图/多来源抽图）"

git push -u origin HEAD


