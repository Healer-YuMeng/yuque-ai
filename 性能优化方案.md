# 对话性能优化方案

> 问题：每次对话耗时约 20s，需定位瓶颈并优化。

---

## 1. 当前耗时分解

以 `POST /chat/v4/stream` 为例，单个轮次链路如下：

| # | 步骤 | 操作 | 耗时 |
|---|------|------|------|
| 1 | 路由 | FastAPI 路由分发 | ~2ms |
| 2 | DB: ensure_session | SQLite 写入/确认会话 | ~10ms |
| 3 | DB: list_recent_messages | 取最近 20 条历史 | ~5ms |
| 4 | DB: append_message(user) | SQLite 写入用户消息 | ~5ms |
| 5 | ProfileExtractor 规则抽取 | 正则匹配 | ~5ms |
| 6 | **ProfileExtractor LLM 兜底** | **LLM chat completion** | **0~5s** |
| 7 | DB: upsert_profile + get_profile | 2 次 SQLite 读写 | ~15ms |
| 8 | 目录状态机 + LeadOutreach | 本地计算 | ~5ms |
| 9 | **MCP search × 2** | **每次启动子进程 + 握手 + 网络请求** | **4~12s** |
| 10 | **MCP get_doc × N** | **每次启动子进程 + 握手 + 并行网络请求** | **3~8s** |
| 11 | DB: touch_focus_docs | SQLite 写入 | ~5ms |
| 12 | **LLM stream_generate 首 token** | **大模型 API 调用** | **3~8s** |
| 13 | LLM 流式输出 | 持续输出 token | 2~5s |
| 14 | DB: append_message(assistant) | SQLite 写入 | ~5ms |

**→ 典型总耗时：18~22s**

---

## 2. 瓶颈根因分析

### 🔴 头号瓶颈：MCP 子进程冷启动（占 60~70%）

`YuqueMCPClient._call_tool()` 每次调用都经过完整的进程生命周期：

```
创建 Node.js 子进程 → stdio 管道连接 → MCP 协议握手初始化 → call_tool → 进程退出
```

这个开销每次约 **0.5~1.5s**（进程创建 + stdio 握手），加上语雀 API 网络延迟 **1~2s**，单次 MCP 调用就 **2~4s**。

一轮对话里 `_retrieve_for_nodes()` 会执行：
- `mcp_client.search()` × 2（共 4~8s）
- `mcp_client.get_doc()` × 3~10（并行后 3~8s）

**仅 MCP 阶段就吃掉 7~16s。**

### 🟡 次号瓶颈：ProfileExtractor LLM 兜底

`ProfileExtractor._extract_by_llm()` 在规则未抽取到新信息时，会用 LLM 做一次完整的 chat completion，耗时 **0~5s**。当前是**串行**等待完毕后才进入检索。

### 🟢 次要瓶颈：SQLite 高频单次读写

每轮 6~8 次独立 SQLite 操作，虽然单次很快（~5ms），但在高并发下写锁会成为瓶颈。

### 🟢 次要瓶颈：Vision LLM 资源竞争

`enrich_media_bundle_with_vision()` 异步发起的 Vision LLM 调用，与主生成 LLM 共享同一 API（百炼），可能触发限流反而拖慢主流程。

---

## 3. 优化方案

### 3.1 P0：MCP 连接池复用（预期节省 6~12s）

**问题**：每次 `_call_tool()` 都创建新的 stdio 子进程和 MCP 会话。

**方案**：改为应用级长连接，整个生命周期只创建一次 MCP 会话。

```python
# mcp_client.py — 改造为上下文管理器 + 连接复用

class YuqueMCPClient:
    def __init__(self, ...) -> None:
        ...
        self._session: Optional[ClientSession] = None
        self._read_stream = None
        self._write_stream = None
        self._initialized = False

    async def __aenter__(self):
        if not self.enabled:
            return self
        server_params = StdioServerParameters(
            command=self._command, args=self._args
        )
        ctx = stdio_client(server_params)
        self._read_stream, self._write_stream = await ctx.__aenter__()
        self._session = await ClientSession(
            self._read_stream, self._write_stream
        ).__aenter__()
        await self._session.initialize()
        self._initialized = True
        return self

    async def __aexit__(self, *args):
        if self._session:
            await self._session.__aexit__(*args)
        if self._read_stream:
            await self._read_stream.__aexit__(*args)
        self._initialized = False

    async def _call_tool(self, tool_name, arguments):
        if not self._initialized:
            raise MCPClientError("MCP 未初始化")
        response = await self._session.call_tool(tool_name, arguments)
        # ... 后续解析不变
```

然后在 `QAService.startup()` 中初始化，并通过依赖注入传给各 Orchestrator。

**效果**：单次 MCP 调用从 2~4s 降至 0.5~1s（仅网络时间）。

**风险**：
- MCP node 子进程崩溃需加自动重连机制
- 长时间 idle 可能导致 stdio 管道断开，需要心跳探活

---

### 3.2 P0：MCP 查询结果缓存优化（预期节省 2~4s）

**现状**：`_retrieve_for_nodes()` 已实现缓存（`_node_retrieval_cache` / `_doc_context_cache`），但 TTL 仅 10 分钟。

**方案**：

| 参数 | 当前值 | 建议值 | 理由 |
|------|--------|--------|------|
| `_DOC_CONTEXT_CACHE_TTL_S` | 900 (15min) | 1800 (30min) | 文档正文很少频繁改动 |
| `_NODE_RETRIEVAL_CACHE_TTL_S` | 600 (10min) | 1800 (30min) | 同一 session 连续追问应命中 |
| 连续追问 `prefer_cached` | 仅 follow_up 触发 | 当 cache hit 时一律跳过 search | 减少 MCP 调用 |

```python
# 增强 _should_skip_mcp_search 逻辑
# 当 direct_doc_hits >= 2 时跳过 MCP search
# 当 prefer_cached 且缓存命中时，仅拉正文不搜索
```

---

### 3.3 P1：ProfileExtractor 与检索并行（预期节省 0~5s）

**现状**：串行等待 LLM 画像提取完成后才进入检索。

**方案**：改为并行执行。

```python
# qa_service.py / sales_dialog_orchestrator_v4.py

# 当前（串行）：
update = await self._extractor.extract_update(...)
# ... DB 写 profile
doc_ctx, sources = await self._retrieve_for_nodes(...)

# 改后（并行）：
extract_task = asyncio.create_task(
    self._extractor.extract_update(...)
)
# 先拉起检索，同时 ProfileExtractor 在后台跑
doc_ctx, sources = await self._retrieve_for_nodes(...)
update = await extract_task  # 大概率已完成
# ... 再用 update 做 DB 写 profile
```

**额外建议**：加一个配置开关 `PROFILE_LLM_ENABLED`，默认 `false`，仅用正则抽取。绝大部分场景正则足够覆盖。

---

### 3.4 P1：MCP get_doc 请求去重与合并（预期节省 1~3s）

**现状**：`_retrieve_for_nodes()` 中 MCP search 结果 + `direct_doc_hits` 可能产生重复 doc_id，导致 `asyncio.gather` 拉取同一文档多次。

**方案**：基于 `doc_id` 去重后再发 `get_doc`。

```python
# 在 _retrieve_for_nodes() 中增加去重
seen_doc_ids: set[str] = set()
deduped_hits = []
for hit in hits[:settings.chat_v15_max_docs]:
    if hit.doc_id not in seen_doc_ids:
        seen_doc_ids.add(hit.doc_id)
        deduped_hits.append(hit)
```

---

### 3.5 P2：DB 操作批量合并（预期节省 ~50ms）

**现状**：每轮 6~8 次独立 SQLite `async` 操作。

**方案**：

| 合并项 | 当前 | 改后 |
|--------|------|------|
| `get_profile` 多次读取 | 2~3 次独立 `await` | 内存缓存 session_id → profile（TTL 1min） |
| `upsert_profile` + `save_catalog_state` | 2 次写 | 合并为 1 次事务写 |
| `append_message(user)` + `ensure_session` | 2 次写 | 在 `ensure_session` 时顺带写消息 |

---

### 3.6 P3：Vision 识读策略优化（预期避免资源竞争）

**现状**：每次有配图时都异步发起 Vision LLM 调用，与主生成竞用 API。

**方案**：
- **默认不做 Vision 识读**，仅通过 `apply_yuque_proxy_to_media` 返回代理 URL 让前端直接展示
- 仅当用户明确"看图""图片内容是什么"时，才触发识读
- 设置 `VISION_ENABLED=false`（如果不需要识读）

---

### 3.7 P3：LLM 调用参数优化

**方案**：

| 参数 | 当前值 | 建议 |
|------|--------|------|
| `max_tokens` (qwen stream) | 340 | 可适当下调至 260（v4 系统 prompt 已限制 3 段精简） |
| `temperature` | 0.08 (qwen) / 0.2 | 保持，影响不大 |

大幅的 system prompt（v4 约 2000+ 字）也是耗时因素之一。可以在不影响回答质量的前提下精简 20~30%。

---

## 4. 预期效果汇总

| 优化项 | 节省时间 | 难度 | 风险 |
|--------|----------|------|------|
| 🔴 MCP 连接复用 | **6~12s** | 低 | 需加重连机制 |
| 🔴 MCP 缓存优化 | **2~4s** | 低 | 低 |
| 🟡 ProfileExtractor 并行 | **0~5s** | 低 | 低 |
| 🟡 MCP get_doc 去重 | **1~3s** | 低 | 低 |
| 🟢 DB 批量合并 | ~50ms | 中 | 需改 repository |
| 🟢 Vision 策略调整 | 避免资源竞争 | 低 | 低 |
| 🟢 System prompt 精简 | ~0.5s | 中 | 可能影响回答质量 |
| **合计** | **9~25s 节省** | | |

---

## 5. 实施优先级建议

```
Phase 1（本周）: MCP 连接复用 + 缓存 TTL 调整
  目标: 20s → 8~12s

Phase 2（下周）: ProfileExtractor 并行 + get_doc 去重
  目标: 8~12s → 5~8s

Phase 3（后续）: DB 合并 + Vision 策略 + prompt 精简
  目标: 5~8s → 3~5s
```

---

## 6. 改前 / 改后对比

| 指标 | 优化前 | Phase 1 后 | Phase 2 后 | Phase 3 后 |
|------|--------|------------|------------|------------|
| MCP 进程开销 | 6~12s | ~1s | ~0.5s | ~0.5s |
| LLM 抽取开销 | 0~5s（串行） | 0~5s（串行） | ~0s（并行） | ~0s |
| LLM 生成 | 3~8s | 3~8s | 3~8s | 2~5s |
| DB 开销 | ~50ms | ~50ms | ~50ms | ~20ms |
| **总耗时** | **18~22s** | **8~12s** | **5~8s** | **3~5s** |
