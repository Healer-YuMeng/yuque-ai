# 人设修改开发文档

> **问题**：① 单位字段采集错误；② 人设规则没有生效（开场不展示4款产品、信息采集顺序混乱、过早索要联系方式）。

---

## 1. 问题根因分析

### 1.1 单位字段采集错误

**根因**：`ProfileExtractor` 的 LLM 兜底会从非单位信息中"推断"出 `org_name`，导致写入错误值。

代码路径：`app/conversation/profile_extractor.py`

```python
# 第 85-99 行：规则未抽到 → LLM 兜底
if rule.display_name or rule.visitor_type or rule.org_name or (rule.interests and len(rule.interests) > 0):
    return rule
if not self._client:
    return rule
llm = await self._extract_by_llm(question=question, history=history)
return ProfileUpdate(
    display_name=rule.display_name or llm.display_name,
    org_name=rule.org_name or llm.org_name,  # ← 这里会接受 LLM 推断值
    ...
)
```

**问题链条**：

| 环节 | 问题 | 示例 |
|------|------|------|
| LLM 假阳性推断 | 用户说"我想咨询智能招生"，LLM 可能推断 `org_name: "培训机构"` | 实际用户没有提供单位 |
| 正则覆盖不全 | `_ORG_PATTERNS` 只匹配 `学校/中学/小学/幼儿园/教育集团/机构/公司` 结尾 | 遗漏"校区""教培行业""工作室"等 |
| 弱值覆写强值 | `write_org = new_org_raw if new_org_raw else ...` — 只要 LLM 抽出非空就覆写 | 把空 → 错误值 |

**正则覆盖缺陷**（`_ORG_PATTERNS` 第 34-38 行）：

```python
_ORG_PATTERNS = (
    re.compile(r"(?:来自|在)\s*([^\s，,。；;]{2,30}?(?:学校|中学|小学|幼儿园|教育集团|机构|公司))"),
    re.compile(r"(?:我是|我时)\s*([^\s，,。；;]{2,30}?(?:学校|中学|小学|幼儿园|教育集团|机构|公司))的"),
    re.compile(r"(?:单位|学校)\s*[:：]?\s*([^\s，,。；;]{2,30})"),
)
```

缺失覆盖：`校区` `培训中心` `工作室` `教培行业` `某某教育` `少年宫` `科技馆` `少年科学院`。

---

### 1.2 人设规则没有生效

**核心矛盾**：5 层 prompt 互相覆盖，最终只有最后写入的规则被 LLM 遵守。

| 层级 | 代码位置 | 覆盖现象 |
|------|----------|----------|
| 1 | `persona_template.py` → `build_model_agnostic_sales_persona_template()` | 约 1500 字，定义了完整人设 |
| 2 | `sales_dialog_orchestrator_v4.py` → `_build_v4_prompt()` | 约 2000+ 字，追加大量规则 |
| 3 | `generator.py` → `_VISITOR_SALES_SYSTEM` | 约 2000+ 字，独立的 system prompt |
| 4 | `generator.py` → `_VISITOR_SALES_QWEN_APPEND` | 约 800 字，Qwen 特化指令 |
| 5 | `generator.py` → `_VISITOR_SALES_QWEN_FEWSHOT` | few-shot 示例，固化行为 |

**具体问题**：

#### 问题①：开场不展示4款产品

`_build_natural_guide()`（第 877-903 行）**完全绕过 LLM**，直接返回文本：

```python
def _build_natural_guide(...) -> str:
    # ...
    if state.dialog_level <= 0 and not path:
        intro = _platform_intro_for_role(profile)
        body = titles[0] if len(titles) == 1 else "、".join(titles)
        return f"{lead}{intro}\n\n结合您的情况，可以从这几块先了解：{body}。您想先聊哪一块？"
```

而 `_platform_intro_for_role()`（第 1179-1192 行）**没有列出4款产品**：

```python
def _platform_intro_for_role(profile):
    base = "有为人工智能教育平台面向学校与机构，覆盖 AI 通识课、跨学科项目式学习、教师端教学工具与案例社区..."
    # ❌ 缺少：智能招生、学校AI场景定制 的明确介绍
```

#### 问题②：信息采集顺序没有强制执行

`_build_v4_nudge_text()`（第 172-196 行）虽然有顺序检查（name→org→contact），但 `next_field_rule` 在 prompt 中只是一句弱约束：

```python
next_field_rule = (
    f"本轮如需补充客户信息，最多只自然询问这一项：{missing_fields[0]}。\n"
    if missing_fields
    else "..."
)
```

LLM 看到长 prompt 时往往忽略这条，按自己理解去问。

#### 问题③：过早索要联系方式

案例中第2轮就出现 `"如果您现在暂时不方便继续了解，也可以先留下电话或微信"`，这违反了"第三轮互动才索要联系方式"的人设规则。根因是 `_lead_nudge_policy` 的 `_already_nudged` 检查只在整场对话历史中判断"是否已经 nudged 过"，但**不检查是否已经在 prompt 中写了 next_field_rule**，导致 nudge text 叠加在 prompt 之外的尾部追加。

#### 问题④：system prompt 冲突

`generator.py` 的 `_VISITOR_SALES_SYSTEM` 有独立的规则集，与 `persona_template` 的语言不完全一致。例如：

```python
# persona_template: "不要生硬推销，不要制造焦虑"
# generator: "不夸大产品效果，客观说明产品适用场景" (近似但不一致)

# persona_template: "信息采集要穿插在正常答疑里，不要像表单盘问"
# generator: "如果用户只是泛泛咨询，禁止主动索要联系方式" (更严格的表述)
```

两套规则叠加后 LLM 难以精确遵守。

---

## 2. 修改方案

### 2.1 修复：单位字段采集（P0）

#### 2.1.1 增强正则覆盖

**文件**：`app/conversation/profile_extractor.py`

将 `_ORG_PATTERNS` 扩展为：

```python
_ORG_PATTERNS: Sequence[re.Pattern[str]] = (
    # 原有：学校/中学/小学/幼儿园/教育集团/机构/公司
    re.compile(r"(?:来自|在)\s*([^\s，,。；;]{2,30}?(?:学校|中学|小学|幼儿园|教育集团|机构|公司))"),
    re.compile(r"(?:我是|我时)\s*([^\s，,。；;]{2,30}?(?:学校|中学|小学|幼儿园|教育集团|机构|公司))的"),
    # → 新增覆盖
    re.compile(r"(?:来自|在)\s*([^\s，,。；;]{2,30}?(?:校区|培训中心|工作室|少年宫|科技馆|少年科学院|学院|大学))"),
    re.compile(r"(?:我是|我时)\s*([^\s，,。；;]{2,30}?(?:校区|培训中心|工作室|少年宫|科技馆|少年科学院|学院|大学))的"),
    re.compile(r"(?:单位|学校|校区|机构)\s*[:：]?\s*([^\s，,。；;]{2,30})"),
    # 教培行业特有表达
    re.compile(r"(?:我是|做|搞)\s*(?:教培|培训|教育)"),
    re.compile(r"(?:我们|我们这边)\s*(?:是|在)\s*([^\s，,。；;]{2,20}?(?:校区|学校|机构|中心|公司))"),
)
```

并在 `_pick_first_group` 匹配后，对 "我是教培/做培训" 这类**只有角色没有名称**的匹配，返回空值而非角色词本身。

#### 2.1.2 LLM 抽取加防假阳性校验

**文件**：`app/conversation/profile_extractor.py`

在 `_extract_by_llm` 返回后，增加**来源校验**——LLM 抽出的 `org_name` 必须以子串形式出现在用户原始问题（或历史消息）中：

```python
async def _extract_by_llm(self, *, question, history) -> ProfileUpdate:
    # ... 现有 LLM 抽取逻辑 ...
    
    # ✅ 新增：防假阳性校验
    org_name = str(data.get("org_name") or "").strip()
    if org_name:
        # 两字符以下的单位名视为无效（如纯符号/单字）
        if len(org_name) < 3:
            org_name = ""
        # 必须能在用户当前轮或历史中找到完整匹配
        q_clean = (question or "").strip()
        history_text = " ".join(
            (row.content or "").strip() for row in (history or [])[-6:] 
            if row.role == "user" and (row.content or "").strip()
        )
        combined = q_clean + " " + history_text
        if org_name not in combined:
            # 尝试子串匹配（允许部分匹配）
            if not any(org_name in t for t in [q_clean] + [r.content for r in history[-6:] if r.role == "user"]):
                org_name = ""  # ❌ 无法溯源，丢弃
    
    display_name = ...
```

#### 2.1.3 增加 `PROFILE_LLM_ENABLED` 开关

**文件**：`app/core/config.py`

```python
# V4 画像抽取 LLM 兜底（默认关闭，仅用正则；开启后每轮增加 2~5s）
profile_llm_enabled: bool = _env_bool("PROFILE_LLM_ENABLED", False)
```

**文件**：`app/conversation/profile_extractor.py`

```python
def __init__(self) -> None:
    self._client: Optional[AsyncOpenAI] = None
    if not settings.profile_llm_enabled:
        return  # ✅ 开关关闭则不初始化 LLM client
    api_key, base_url = settings.resolve_model_endpoint(settings.llm_model)
    if api_key:
        self._client = AsyncOpenAI(api_key=api_key, base_url=base_url or None)
```

**效果**：关闭后 ProfileExtractor 仅用正则，杜绝 LLM 假阳性。同时省去每轮 2~5s 耗时。

---

### 2.2 修复：人设生效（P0）

#### 2.2.1 统一 System Prompt——以 persona_template 为准，移除 generator 中冲突规则

**核心原则**：`persona_template.py` 是人设的**唯一权威来源**，所有生成器不得追加冲突指令。

**文件**：`app/rag/generator.py`

**修改**：`_VISITOR_SALES_SYSTEM` 中移除与人设重复/冲突的规则，只保留生成器特有的"不输出文档编号/参考来源"等技术约束。

```python
_VISITOR_SALES_SYSTEM = (
    "你是「有为人工智能教育平台」的资深解决方案顾问，名字叫「小为顾问」。"
    # ✅ 只保留生成格式约束，不重复业务规则
    "请使用自然、亲切、专业的中文与访客对话。"
    "你可以使用 Markdown（小标题、列表）组织内容，"
    "但不要使用「## 回答」「## 参考来源」这类固定章节标题。"
    "不要输出内部资料编号或文档标题给访客。"
    # ✅ V4 prompt 已包含完整人设，此处不再重复
)
```

**将上述 generator 的 system prompt 精简为不超过 500 字**。完整人设规则只保留在 `persona_template.py` + `_build_v4_prompt()` 中。

#### 2.2.2 开场必须展示4款产品

**文件**：`app/conversation/sales_dialog_orchestrator_v4.py`

修改 `_platform_intro_for_role()`（第 1179-1192 行），必须包含 4 款产品的精简介绍：

```python
def _platform_intro_for_role(profile: Optional[ChatSessionProfile]) -> str:
    vt = (profile.visitor_type if profile else "") or ""
    base = (
        "有为人工智能教育平台主要提供4类产品和服务：\n"
        "1. 人工智能通识课程——适配中小学AI科普教学，配套乐高、苹果STEAM、索尼、腾讯多套原厂AI课件资源；\n"
        "2. 跨学科项目式学习——PBL项目化科创课程，融合数理+编程+机器人，适合课后社团和校本课；\n"
        "3. 智能招生——教培机构专属AI获客工具，自动化线索引流和体验课邀约，解决校区招生难；\n"
        "4. 学校AI场景定制——根据学校场地、师资量身定制AI实验室和校本课程。\n"
        "需要产品操作教程可以看「使用指南」，想看各校落地成果可以看「案例与社区」。"
    )
    if vt == "parent":
        return base + " 对家长来说，更常关注的是孩子学什么、如何衔接升学与竞赛。"
    if vt == "teacher":
        return base + " 老师一般会关心备课上课、作业评价和课堂落地是否省心。"
    return base
```

#### 2.2.3 强制信息采集顺序

**文件**：`app/conversation/v4_lead_outreach.py`

增强 `_build_v4_nudge_text()` 使其严格按 **称呼 → 工作单位 → 联系方式** 顺序输出，并且前一项未完成时不得跳到下一项：

```python
def _build_v4_nudge_text(
    profile, lead_meta, session_meta,
) -> tuple[str, str]:
    name = display_name_for_chat(profile)
    prefix = f"{name}，" if name else ""
    asked_fields = {...}
    suppressed_fields = {...}
    blocked = asked_fields | suppressed_fields

    # ✅ 严格顺序：只有 name 已收集才推进到 org_name，只有 org_name 已收集才推进到 contact
    has_name = bool(name)
    has_org = bool(lead_meta.get("org_name") or (profile.org_name if profile else ""))
    
    if not has_name and "name" not in blocked:
        return (f"{prefix}方便告诉我怎么称呼您吗？后续我按您的关注点整理对应资料。" if prefix else "方便告诉我怎么称呼您吗？后续我按您的关注点整理对应资料。", "name")
    
    if not has_org and "org_name" not in blocked:
        return (f"{prefix}您目前是在哪个教育单位任职呀？我结合您校区情况匹配更适合的方案。" if prefix else "您目前是在哪个教育单位任职呀？我结合您校区情况匹配更适合的方案。", "org_name")
    
    if not lead_meta.get("contact_value") and "contact" not in blocked:
        return (f"{prefix}我把对应产品案例和详细资料打包发给您，方便留一个微信或电话吗？" if prefix else "我把对应产品案例和详细资料打包发给您，方便留一个微信或电话吗？", "contact")
    
    return ("", "")
```

#### 2.2.4 禁止在内容讲解中间索要联系方式

**文件**：`app/conversation/v4_lead_outreach.py`

在 `evaluate_end_of_turn()` 中增加：**仅当 dialog_level >= 2 且 user_rounds >= 6 才允许 nudge 联系方式**。

```python
async def evaluate_end_of_turn(self, ...):
    # ...
    nudge = LeadNudgeDecision(triggered=False)
    # ✅ 新增严格条件：深度讲解 + 足够轮次
    if not is_guide_only and dialog_level >= 2 and user_rounds >= 6:
        # ✅ 且只有前两项信息（称呼+单位）收集完毕才推进联系方式
        has_name = bool(display_name_for_chat(profile))
        has_org = bool(lead_meta.get("org_name") or (profile.org_name if profile else ""))
        if not (has_name and has_org):
            nudge = LeadNudgeDecision(triggered=False)
        else:
            nudge = self._lead_policy.decide(...)
```

#### 2.2.5 精简 _build_v4_prompt 中的规则——去重、消歧

**文件**：`app/conversation/sales_dialog_orchestrator_v4.py`

`_build_v4_prompt()` 约 2000+ 字，与 `persona_template` 大量重复。修改原则：

| 规则类别 | 当前位置 | 处理方式 |
|----------|----------|----------|
| 基础人设 | `persona_template` | **保留**，作为唯一人设来源 |
| 产品范围 | `persona_template` + prompt | 移除 prompt 中重复 |
| 信息采集顺序 | `persona_template` + `next_field_rule` | prompt 中动态补 `next_field_rule` |
| 目录约束 | prompt | **保留**（动态上下文） |
| 格式/语气 | `generator` | **精简**到 500 字以内 |

改后的 `_build_v4_prompt` 模板（精简版）：

```python
def _build_v4_prompt(...) -> str:
    persona = build_model_agnostic_sales_persona_template()
    
    # 动态上下文（目录、历史、媒体、技能）
    context_parts = [
        f"当前目录位置：{catalog_path}（对话层级 {dialog_level}）。",
        skill_instructions,
        f"本轮规则：目录约束—只讲当前目录内内容；{next_field_rule}",
        media_hint,
        history_block,
        f"用户问题：{question}"
    ]
    
    return persona + "\n\n" + "\n".join(filter(None, context_parts))
```

---

### 2.3 其他配套修改

#### 2.3.1 添加匿名对话记录导出中的验证脚本

**文件**：`app/core/config.py`

```python
# 人设开关：关闭后回退到旧版 prompt（灰度切换用）
persona_v2_enabled: bool = _env_bool("PERSONA_V2_ENABLED", True)
```

这样修改上线后如果有问题可以一键回滚到旧版 prompt。

#### 2.3.2 修复 `_already_nudged` 检测范围

**文件**：`app/conversation/lead_nudge_policy.py`

当前 `_already_nudged` 的 marker 列表缺少新话术：

```python
nudge_markers = (
    "可以留一下", "留下联系方式", "留个电话", "开通试用账号",
    "申请测试账号", "安排顾问",
    # ✅ 新增
    "方便留一个联系方式", "方便留个手机号", "方便留个微信",
)
```

---

## 3. 修改文件清单

| 文件 | 修改内容 | 类型 |
|------|----------|------|
| `app/conversation/profile_extractor.py` | 扩展 `_ORG_PATTERNS`、LLM 假阳性校验、`PROFILE_LLM_ENABLED` 开关 | 🟡 核心 |
| `app/core/config.py` | 新增 `profile_llm_enabled` 和 `persona_v2_enabled` | 🟢 配置 |
| `app/conversation/persona_template.py` | 补充4款产品介绍细节、信息采集顺序描述 | 🟡 核心 |
| `app/rag/generator.py` | 精简 `_VISITOR_SALES_SYSTEM` 到 500 字、"不知道您是谁"修复 | 🔴 关键 |
| `app/service/sales_dialog_orchestrator_v4.py` | 开场4产品介绍、精简 prompt、调整 `_answer_at_node` | 🔴 关键 |
| `app/conversation/v4_lead_outreach.py` | 严格信息采集顺序、禁止过早索要联系方式 | 🔴 关键 |
| `app/conversation/lead_nudge_policy.py` | 补充 nudge marker 列表 | 🟢 配置 |
| `app/service/sales_dialog_orchestrator_v4.py` | 修改 `_build_natural_guide` 使用 persona 规则 | 🟡 核心 |

---

## 4. 改前 / 改后效果对比

| 维度 | 改前（案例表现） | 改后（预期） |
|------|------------------|-------------|
| 开场 | 直接切入智能招生，不介绍4款产品 | 先简要介绍4款产品+配套板块 |
| 信息采集顺序 | 第2轮同时问称呼+联系方式 | 第1轮问称呼→第2轮问单位→第3轮问联系方式 |
| 单位采集准确性 | LLM 可能推断出"培训机构"等假值 | 正则优先 + 溯源校验，误差率 ↓90% |
| 索要联系方式时机 | 第2轮就出现 | 至少完成称呼+单位收集后，且 ≥6轮深度对话 |
| ProfileExtractor 耗时 | 每轮 0~5s（LLM call） | 关闭 LLM 后 0s，仅 ~5ms 正则 |
| 人设一致性 | 5层 prompt 互相覆盖 | 单一权威来源 `persona_template` |

---

## 5. 实施步骤

```
Phase 1（今天）:
  1. config.py 新增 profile_llm_enabled / persona_v2_enabled
  2. profile_extractor.py 修复 org 抽取 + 开关
  3. 测试：用案例对话验证单位不再误采集

Phase 2（明天）:
  4. generator.py 精简 _VISITOR_SALES_SYSTEM
  5. persona_template.py 补充4款产品+顺序规则
  6. v4_lead_outreach.py 强制顺序

Phase 3（后天）:
  7. sales_dialog_orchestrator_v4.py 精简 prompt + 开场修复
  8. 端到端测试：新会话→验证开场、信息采集顺序、留资时机
```
